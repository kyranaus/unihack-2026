import { addTodo, listTodos } from './todos'
import {
  startSession,
  analyseRoadFrames,
  logDriverEvent,
  endSession,
  getSession,
  getProfileStats,
  getLeaderboard,
  getVideoUploadUrl,
  getVideoDownloadUrl,
  listDriveSessions,
  initVideoUpload,
  getPartUrl,
  completeVideoUpload,
  abortVideoUpload,
  storeVideoHash,
  verifyVideoHash,
} from './drives'

export default {
  listTodos,
  addTodo,
  startSession,
  analyseRoadFrames,
  logDriverEvent,
  endSession,
  getSession,
  getProfileStats,
  getLeaderboard,
  getVideoUploadUrl,
  getVideoDownloadUrl,
  listDriveSessions,
  initVideoUpload,
  getPartUrl,
  completeVideoUpload,
  abortVideoUpload,
  storeVideoHash,
  verifyVideoHash,
}
